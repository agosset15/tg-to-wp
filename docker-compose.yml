services:
  bot:
    build: .
    container_name: tg-to-wp-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - 127.0.0.1:${PORT:-9155}:${PORT:-8443}
    logging:
      driver: json-file
      options:
        max-size: 100m
        max-file: 5